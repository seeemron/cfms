{
  "name": "cfms-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build": "cd client && yarn install && yarn build && cp -r build/* ../public/",
    "postinstall": "echo postinstall",
  }
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "pg": "^8.10.0",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.2",
    "better-sqlite3": "^9.0.0"
  }
}
